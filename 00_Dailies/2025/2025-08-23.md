- 3. tau 当成 分割点, 不要用vocab向量来做
	- forall tau_i, theta_1 ... , 直接判断是否落在某个区间
- **theta > 0 试一下 clip**
- 实验的 setting 还是比对 minp 来做, 这样直接用他们的 work 的设置即可
- **2. 将 truncate 改成 x/p_max >= tau; else**
- **1. 看一下 minp 最后的归一化是怎么做的, 总而言之是能够退化成minp的**