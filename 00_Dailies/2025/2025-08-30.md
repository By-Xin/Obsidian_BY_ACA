在 AI 这种 vibe coding 环境下, 信息比以往还要过分地爆炸. 因此尤其地会有 less is more. 噪音太多了, 不缺少信息, 缺少 filter. 以后的生产不在于创造, 而在于筛选, 整理与消灭. 在工作中更是如此. 一定要保持好习惯, 做好日志, 让生产过程可追寻. 一定不要被 kipple 吞没. 不管是任何形式的记录, 例如 GitHub 中代码的跟踪怎样都好, 一定要有一个 meta notebook, 掌管最底层的逻辑. 这个真的太重要了. 同样也不要放到脑子里, 不然就是一团乱麻. 
- 因此, 未来我们更需要: 压缩, 降维, 整理, 筛选. 已经有足够强大的创造寡头了 (AIGC), 但是信息的整理还远远不足. 

---


VCC 方面:
- 现在的主要任务是跑通 eval. 
	- 我们拥有 CPU 节点, 但是数据上传的速度太慢了. 我们已经尝试将测评代码上传, 但是数据文件上传太慢了. 
		- 要么上传 baselin.vcc 或者其他任何待提交的文件, 但是这个是一次性的.
		- 最根本是上传原始的训练集和测试集的数据. 
	- 要么就是在 GPU 节点上尝试运行我们的 GPU 魔改版本. 这个版本由 claude-code 完成. 主要是 DE 部分应当并行的部分比较多. `vcc_gpu_accelerator`是最主要的 gpu 接口文件. `STEP_BY_STEP_GUIDE.md` 相当于对应生成的一个使用说明. 
- 尝试整理上周留下的烂摊子. 现在的主要任务是弄清我们的结构. 现在有两个主要的目录数据目录如下. 我们需要弄清其中的逻辑.  

```
(torch) sczd425@ln01:~/run/xinby/competition_support_set$ ls
aaa.sh  competition_val_template.h5ad  gene_names.csv  k562.h5              s2.py         t2.py               vcc_eval_gpu.py
baseline_eval_gpu.json       des_debug.py        gpu_eval.sh     multi_eval.sh                 s3.py         test.py             vcc_multi_gpus_official.py
check.py                     ESM2_pert_features.pt          hepg2.h5        README_official_gpu.md        s4.py         truth_val.h5ad      vcc_multi_gpus.py
competition_support_set.zip  eval.sh                        
jurkat.h5       replogle_eval_processed.h5ad  srun.sh       truth_val_raw.h5ad  vcc_offline_eval.py
competition_train.h5         final_run.sh                   k562_gwps.h5    rpe1.h5                       starter.toml  val_perts.json      wget-log

(torch) sczd425@ln01:~/run/xinby/cell-eval/vcc_data$ ls
adata_Training.h5ad  baseline.h5ad  baseline.prep.vcc  gene_names.csv  pert_counts_Validation.csv
```


---


  重叠率: 50/50 = 100% ✅
  - 官方验证集的所有50个扰动在support set中都有对应！
  - Support set只多了一个non-targeting（对照组）

  🎯 这意味着什么？

  1. 完美的本地评估机会
  - **<font color="#c00000">competition_val_template.h5ad就是官方为VCC验证集创建的标准参考</font>**
  - 我们可以用它作为"ground truth"进行本地评估

  2. 不是数据泄露，而是官方支持
  - 这显然是Arc Institute官方提供的评估支持数据
  - STATE示例就是用这个进行本地测试的
  - 这是为了让参赛者可以在本地调试pipeline

  3. 评估策略确定
  训练: vcc_data/adata_Training.h5ad (151个扰动)
  预测: 基于pert_counts_Validation.csv生成50个扰动的预测
  评估: 用competition_val_template.h5ad作为"真实值"
  对比: 本地VCC分数 vs 官方leaderboard分数

  💡 建议的下一步

  让我们创建一个本地评估脚本：

  # 伪代码框架
  def local_vcc_evaluation():
      # Step 1: 使用您已生成的baseline预测
      baseline_pred = "baseline_output/baseline_prediction.h5ad"

      # Step 2: 使用competition_val_template作为真实值
      ground_truth = "competition_support_set/competition_val_template.h5ad"

      # Step 3: 运行VCC评估
      evaluator = MetricsEvaluator(
          adata_pred=baseline_pred,
          adata_real=ground_truth,
          profile="vcc"  # 使用VCC专用指标
      )

      # Step 4: 获得本地VCC分数
      results, agg_results = evaluator.compute()

      # Step 5: 与官方分数对比验证

  预期结果：
  - 如果本地VCC分数与您上传官网得到的分数接近，说明评估系统有效
  - 后续可以用这个系统快速评估模型改进

---
- vcc_baseline_evaluation.py 就是基于这个 support set 开发的!